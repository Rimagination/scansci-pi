{
  "name": "scansci-pi",
  "private": true,
  "version": "0.2.0",
  "description": "ScanSci desktop workbench powered by the Pi Agent SDK.",
  "scripts": {
    "build:pi-runtime": "esbuild pi-runtime/src/main.ts --bundle --platform=node --format=esm --target=node22 --banner:js=\"import { createRequire as __scansciCreateRequire } from 'node:module'; const require = __scansciCreateRequire(import.meta.url);\" --outfile=pi-runtime/dist/main.mjs",
    "vendor": "python scripts/vendor_web_assets.py",
    "render:slide-plan": "node scripts/render_slide_plan.mjs"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.80.10",
    "pdfjs-dist": "6.1.200",
    "pptxgenjs": "4.0.1",
    "typebox": "1.1.38"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
